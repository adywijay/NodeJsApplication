/* 
 * array.forEach(function(currentValue, index, arr))
 */


let students = ['John', 'Sara', 'Jack'];

students.forEach((params)=>{
    console.log(params);
});

//Manipulation array
let std1 = ['John', 'Sara', 'Jack'];

std1.forEach((item,index,arr)=>{
    console.log([index] = 'Mokudzaa',item);
});
